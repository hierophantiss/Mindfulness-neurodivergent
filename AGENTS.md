# Σημαντικοί Κανόνες για το Project (Mindfulness / Serene)

## 1. Διαχείριση Αρχείων Ήχου (Audio & Binary Files Blind Spot)
* **ΑΥΣΤΗΡΟΣ ΚΑΝΟΝΑΣ**: Απαγορεύεται ρητά η προσπάθεια αντιγραφής, μεταφοράς, λήψης ή επεξεργασίας δυαδικών αρχείων (binary files: `.mp3`, `.mp4`, `.pdf`, εικόνες) μέσα στο περιβάλλον εργασίας (π.χ. μέσω `node`, `fs.copyFileSync`, `shell_exec`). 
* **Γιατί**: Στο συγκεκριμένο περιβάλλον, οι εντολές αυτές αλλοιώνουν την κωδικοποίηση των αρχείων, με αποτέλεσμα να τα καταστρέφουν εντελώς. 
* **Λύση**: Όλα τα αρχεία ήχου υπάρχουν ήδη σωστά τοποθετημένα μέσα στον φάκελο `public/music/` από το εξωτερικό GitHub repository. Ο κώδικας (π.χ. React components / Contexts) πρέπει απλώς να αναφέρεται σε αυτά απευθείας (π.χ. `url: "/music/track.mp3"`), χωρίς καμία παρέμβαση στα ίδια τα αρχεία από το AI.

## 2. Γενικοί Κανόνες Σχεδιασμού (Design System)
* **Χρώματα**: ΜΟΝΟ από τις CSS μεταβλητές του `src/index.css` (π.χ. `--color-bg: #0f1117`). Ποτέ μη χρησιμοποιείς φωτεινό λευκό (`#fff`) για φόντο.
* **Δομή**: Vanilla HTML/CSS/JS και React / TS. Responsive first (375px βάση).
* **UI/UX Καθαρότητα**: Το design πρέπει να είναι premium, ήρεμο και απολύτως καθαρό. Απαγορεύονται τα logs στην οθόνη, τα άχρηστα tech-labels και οτιδήποτε "μοιάζει με AI".
