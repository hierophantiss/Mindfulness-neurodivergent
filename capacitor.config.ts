import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.neurodivergentmindfulness.app',
  appName: 'Neurodivergent Mindfulness',
  webDir: 'dist'
};

export default config;
